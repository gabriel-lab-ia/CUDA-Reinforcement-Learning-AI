suite: ppo-microbenchmark
benchmark_type: ppo_microbenchmark
algorithm: ppo
environment_id: null
backends: [torch_cpu, torch_cuda]
seeds: [0, 1, 2, 3, 4]
episodes: 25
device: cpu
dtype: float32
mode: smoke
output_directory: reports/benchmarks
hyperparameters:
  batch_size: 512
  repeats: 25
telemetry:
  enabled: true
  interval_seconds: 1.0
